spring.application.name=auth-service

server.port=8084
#postgresql configuration:

spring.datasource.url=jdbc:postgresql://213.42.28.16:5432/sima_mis
spring.datasource.username=postgres
spring.datasource.password=PNB@PRG$MRT25TM
spring.datasource.driver-class-name=org.postgresql.Driver

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost
eureka.instance.instance-id=${spring.application.name}:${server.port}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# JWT Configuration
app.jwt.secret=mySuperSecretKeyThatIsAtLeast32CharactersLong123!
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Logging
logging.level.com.mis.auth_service=DEBUG